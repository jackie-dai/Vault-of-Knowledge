
## Structure: 
command option(s) argument(s)


```
cd or cd~ # switches to home directory
```

```
rm # removes files
rm -r removes directories and everything inside them
```


```
wc notes_final.txt
1 2 9 notes_final.txt
```
line count, word count, character count

